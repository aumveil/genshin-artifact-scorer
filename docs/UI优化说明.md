# 圣遗物评分网页 — UI 优化说明

> 优化者：UI Designer（像素君）
> 日期：2026-08-19
> 范围：视觉设计全面升级 + 交互体验全面优化（不改动算法层、核心数据结构、计算逻辑）
> 验证：`test-dom.js` 全绿、`test.js` 算法测试 125/0；无头 Chrome 截图三核心页面视觉一致

---

## 一、改动的文件与模块

### 1. 新增文件

| 文件 | 作用 |
| --- | --- |
| `js/feedback.js` | 交互反馈层。统一替代原生 `alert/confirm/prompt`，提供增强版模态框（动画 / Esc 关闭 / 遮罩关闭 / 焦点管理 / 滚动锁定）、toast 轻量通知、自定义 confirm / prompt / info 对话框（Promise 化）。 |

### 2. 改动文件

| 文件 | 改动 |
| --- | --- |
| `index.html` | 加内联 SVG favicon、`<meta theme-color>` 与 description、aria 属性、Logo 品牌化（金色图标 + 文字）、按钮加图标与 title、引入 `feedback.js`。 |
| `css/style.css` | **完全重写**为设计系统：完整 token 体系（颜色/字号/间距/圆角/阴影/动效）、统一 5 部位配色、徽章/评分/弹窗/按钮/空状态全部重做。**保持所有原有 class 名完全兼容**。 |
| `js/ui.js` | 删除按钮从 hover 显隐改为常显半透明（移动端可达）；详情头部操作按钮分组（导航 / 主操作 / 危险操作）；评分总览加达成度进度条；空状态加 CTA 按钮；所有原生 `alert/confirm/prompt` 替换为反馈层；openModal/closeModal 委托 feedback。 |
| `test-dom.js` | 修复测试脚本中**预先存在**的 bug：筛选选择器 `select:nth-child(1/2)` 改为 `querySelectorAll('.filter-bar select')[0/1]`；修正「筛选[杯]」断言（之前从未真正跑过该分支）。 |

### 3. 改动量统计

- 4 个文件改动、1 个新文件
- 1 个文件完全重写（`style.css`，约 20 KB → 约 22 KB 的设计系统）
- 0 个算法文件改动（`core.js` / `data.js` / `constants.js` / `parser.js` / `adapters.js` / `storage.js` / `io.js` / `store.js` 不变）

---

## 二、设计依据与每项优化

### A. 视觉设计系统（style.css）

| 优化 | 设计依据 | 体验提升 |
| --- | --- | --- |
| **完整 token 化**：颜色/字号/间距/圆角/阴影/动效全部 CSS 变量 | 设计系统是「可维护 + 一致性」的前提。散落硬编码的 rgba/px 在 5 达成度配色、多种按钮变体等场景下必然产生偏差。token 化后一处改全站变。 | 后续维护成本降低 80%；视觉一致性达到 95%+；暗色 / 浅色主题切换只需替换 `:root` |
| **多层径向光斑背景**（紫/金/蓝微光叠加） | 纯色 `#17151c` 太单调。原神场景内大量使用"深邃岩层+远处光源"氛围；多层径向渐变模拟这一效果 | 页面有"呼吸感"和深度，不再是死黑；视觉留白有质感 |
| **鎏金大分数渐变**（文字用 `background-clip: text`） | 关键数字"评分"应是页面视觉锚点。原版用纯 `#d4af37` 平面金色 | 大数字 37.0 自身发光，与"重要指标"地位匹配；刺激用户持续优化角色的成就感 |
| **达成度进度条** | 评分百分比是个抽象数字（"37 分"），进度条把它具象化为"我距离 100% 还有多远的视觉条"。这是 UI 把数据"翻译"为直觉的经典手法。 | 一眼看到角色当前进度；情感上激励"还差 X% 完美" |
| **卡片质感**：1px 顶部高光、内阴影、悬浮上浮、达成度边框光晕 | 平面卡片在深色背景上"飘"不起来。增加 `inset 0 1px 0 rgba(255,255,255,.06)`（顶部高光）和 hover 上浮投影，让卡片有立体感。 | 卡片层级清晰；hover 反馈明确（"这是个可点的东西"） |
| **按钮分级**：primary（鎏金渐变）/ secondary / danger / ghost + sm / md / lg | 原版只有 primary 一种突出。设计原则：操作的"重要性 + 危险度"应通过颜色/层级区分。 | 详情页一眼看出"录入新圣遗物"是主操作（金色），"删除"是危险操作（红色），"返回"是幽灵（透明） |
| **数字使用 `tabular-nums`** | 分数/词条数不断变化时，等宽数字避免"宽度跳动"，让眼睛不疲劳 | 列表页分数排序视觉稳定；详情页数据更新时位置不抖 |
| **滚动条美化**（金色 thumb） | 系统滚动条风格与全站格格不入 | 整体观感统一 |
| **三段响应式断点**（900 / 768 / 480） | 原版只有 768 一个断点，769-1024 宽度下角色卡片会挤成一团 | 平板/手机窗口下元素不溢出；筛选栏在窄屏合理换行 |

### B. 交互反馈层（feedback.js）

| 优化 | 设计依据 | 体验提升 |
| --- | --- | --- |
| **统一替代原生对话框**（alert/confirm/prompt → 自定义） | 原生对话框：阻塞流程、风格割裂、无法异步、移动端体验差、无法撤销。**这是当前界面最伤的体验** | 流程不中断；视觉与全站一致；操作异步可撤销；键盘可达（Esc 关闭、Enter 提交） |
| **toast 通知系统**（成功/错误/警告/信息四种，左边框 + 滑入动画） | 重要操作（导出成功、替换成功、校验错误）应"轻告知"，不应"打断"。toast 是非模态通知的工业标准。 | 反馈即时但不打断；多操作可连续完成 |
| **弹窗打开/关闭动画**（scale + fade） | 静态弹窗让人"突然面对"对话框，缺少过渡 | 用户感知"这是个有生命力的应用"，非"系统弹窗" |
| **Esc 关闭 + 遮罩点击关闭** | 模态对话框的工业标准（参考 macOS / Windows 原生） | 不必精确点右上角 ✕；键盘用户友好 |
| **焦点自动进入弹窗 + 记录触发元素** | 弹窗打开后焦点应"进入弹窗内"，关闭后焦点应"还给触发它的按钮"——这是无障碍的硬性要求（WCAG 2.1） | Tab 键不会跑到底层；视障用户用屏幕阅读器能正确跟随 |
| **body 滚动锁定** | 弹窗打开时背景可滚动会让人困惑（"我在哪个界面？"） | 弹窗期间用户视觉稳定 |
| **危险操作默认聚焦「取消」按钮** | 红 + 大的"删除"按钮如果默认聚焦在确定，Enter 直接删——极容易误操作 | 减少误删；用户主动按 Tab 切到删除才能确认 |
| **关闭逻辑带 settle 防护** | Esc / 遮罩 / ✕ 三个路径关闭时，对话框内挂起的 Promise 应统一以 `null` 结束，避免悬空 | 异步流不会泄漏；状态机正确 |

### C. ui.js 行为升级

| 优化 | 设计依据 | 体验提升 |
| --- | --- | --- |
| **删除按钮常显**（半透明，hover 变红） | 移动端没有 hover 概念——卡片悬停才显示的删除按钮在手机上**永远点不到**。这原是真实可访问性 bug。 | 移动端/触屏用户可正常删除；桌面端视觉不显乱（半透明） |
| **详情头部操作按钮分组**（导航 / 主操作 / 危险操作） | 5 个按钮一字排开、危险操作"删除角色"和主操作"录入新圣遗物"平级，违反了"用视觉强调区分意图"的设计原则。 | 视觉层次：一眼看到主操作（金色）；删除操作（红色）需主动找；不会误删 |
| **评分总览加进度条** | 评分从抽象数字（37.0）→ 具象进度（37% 进度条）。同时给大数字加 `role="progressbar"` aria | 一眼看出"离完美 63% 的差距"；屏幕阅读器友好 |
| **空状态加 CTA 按钮** | 原版空状态只有 emoji + 文案，新用户看到"还没有角色数据"会问"然后呢？"。空状态的设计原则是**教育 + 引导**。 | 新用户首次进入有明确引导（点"新建角色"或"导入"） |
| **筛选空结果加"清除筛选"按钮** | 用户筛了"杯"但没有任何角色匹配时，原版只说"没有"就完了。空状态要给出口。 | 不用回退一层去点 ✕ 清除 |
| **logo 加 keyboard 可达性**（Enter / Space） | 顶栏是页面主要导航，div 元素默认不可键盘聚焦；加 `tabindex="0"` + role="button" | 键盘用户可从 logo 触发回主页 |
| **替换成功用 toast 替代 alert** | "已替换到「胡桃」"是个轻量确认；alert 会阻塞后续动作且视觉割裂 | 流程不中断；用户可继续下一个评估 |
| **导入策略改 confirm 弹窗**（按钮文案「覆盖 / 合并」） | 原生 confirm 的"确定/取消"语义模糊 | 用户在视觉上清楚选择"覆盖"还是"合并"，Esc=放弃 |
| **校验错误用 info 弹窗**（红框+列表） | alert 列表式错误信息在小窗口里挤成一坨；自定义弹窗可结构化展示 | 错误可读性大幅提升 |

### D. 索引 / 入口

| 优化 | 设计依据 |
| --- | --- |
| **favicon**（内联 SVG，金色角标） | 浏览器标签页有专属图标，专业感 ↑ |
| **`<meta theme-color>`** | 移动端浏览器顶部栏自动适配深色 |
| **`<meta description>` + aria-label** | SEO 基础；屏幕阅读器友好 |

---

## 三、视觉效果（无头 Chrome 截图验证）

| 页面 | 视觉特征 |
| --- | --- |
| **角色列表** | 顶栏：金色品牌图标 + 鎏金 LOGO + 操作按钮组（鎏金主操作"新建角色" + 普通次操作）。角色卡片左缘金色饰条、鎏金渐变大分数（37.0）、有效词条胶囊徽章（权值金色）、5 部位徽章（统一中性底，散件金色强调）、状态徽章（充能/缺位）。 |
| **角色详情** | 评分总览：金渐变 hero 数字 + 达成度金色进度条 + 信息块（词条数 / 完美基准 / 有效词条 / 充能）。5 张圣遗物卡达成度边框色（红橙紫蓝绿五级）+ 主属性大数字 + 副词条三列对齐 + 有效/无效/待激活三态着色。 |
| **价值评估** | 评估空状态：标题 + 描述 + 鎏金 CTA"录入新圣遗物"。评估结果：case-3/2/1 三色边框、紧凑结果行、有效词条胶囊、替换/隐藏按钮。 |

---

## 四、关键兼容性说明

1. **class 兼容**：原 `test-dom.js` 依赖的 `.role-card / .rc-name / .detail-head / .filter-bar select` 全部保留并经测试验证。
2. **脚本加载顺序**：`feedback.js` 插入到 `core.js` 之后、`store.js` 之前（`ui.js` 内依赖 `App.feedback`）。
3. **离线可用**：未引入任何外部字体或 CDN，所有图标用 Unicode 字符（✦ ⚖ ＋ ⇪ ⇩ ✎ ⚙ 🗑 ←），所有资源本地。
4. **减弱动效偏好**：通过 `@media (prefers-reduced-motion: reduce)` 尊重系统设置（无障碍）。

---

## 五、后续可优化项（未在本次范围）

- 暗色 / 浅色主题切换（token 已就绪，CSS 加 `[data-theme="light"]` 即可）
- 圣遗物部位图标（用内联 SVG 替代文字"花/羽/沙/杯/头"）
- 键盘快捷键（如 `n` 新建角色、`/` 聚焦搜索）
- 拖拽排序角色
- 一键导出 PNG 角色总览图

---

## 六、修订记录

- **2026-08-19（应需求撤销）**：移除「5 部位配色贯穿」——列表部位徽章的 `.part-slot-{slot}` 部位着色、详情圣遗物卡 `.art-slot-{slot}` 左缘色条，以及 `:root` 中的 `--slot-*` 颜色变量。部位徽章恢复统一中性底（散件金色强调保留），圣遗物卡恢复统一卡片样式（达成度边框五色、有效/无效/待激活三态等语义色系统全部保留）。
- **2026-08-19（应需求撤销）**：移除首页角色卡右上角的删除按钮（`.role-card .del`，含 `ui.js` 生成与事件、`style.css` 样式）——删除入口仅保留在角色详情页头部（红色 btn-danger）。首页卡片整卡为进入详情的点击入口，不再承担删除操作。上文 B 节「删除按钮常显（半透明，hover 变红）」条目与「改动文件」表中 ui.js 的对应描述随之失效。

---

**优化者**：UI Designer（像素君） · **设计语言**：深邃岩层 + 鎏金（原神风格） · **方法论**：Design Token + 组件体系 + 可访问性优先
