# 圣遗物评分网页 · 实现方案

> 基于已定稿的《圣遗物背景知识总结.md》与《圣遗物评分算法设计.md》实现。
> 目标：一个本地使用的圣遗物管理网页，覆盖数据管理、计算、评估与展示全流程。

---

## 一、项目概述

### 1.1 功能清单（用户需求）

| # | 功能 | 说明 |
|---|------|------|
| 1 | 角色圣遗物数据导入 / 导出 | 完整 JSON 导出备份、导入恢复（可选覆盖/合并） |
| 2 | 单件圣遗物词条计算 | 每件圣遗物的加权有效词条数（算法一） |
| 3 | 角色圣遗物评分 | 角色总分 + 完美态百分制 + 充能标签（算法二） |
| 4 | 圣遗物价值评估 | 新圣遗物相对已穿戴件是否有价值：情况一散件 / 情况二套装替换 / 情况三套装件替换散件，含未满级强化潜力评估（算法三） |
| 5 | 数据与结果展示 | 角色列表、5 部位详情、评分卡片、评估结果，全部可视化 |

### 1.2 设计原则

- **零部署**：双击 HTML 即用，数据存浏览器 localStorage
- **离线可用**：无后端、无外部 CDN 依赖（保证本地打开不联网也能用）
- **多角色**：支持多个角色独立管理
- **所见即所得**：录入即实时计算，修改即时报错

---

## 二、技术实现方案

### 2.1 技术选型

| 项 | 选择 | 理由 |
|----|------|------|
| 语言 | HTML5 + CSS3 + 原生 JavaScript（ES6） | 零构建、零依赖，双击即用 |
| 持久化 | localStorage | 本地存储，容量约 5MB，足够角色+圣遗物数据 |
| 结构 | 多文件模块（script 按序加载） | 无打包工具时用全局命名空间组织 |
| 图标/字体 | 内联 SVG + 系统字体 | 避免外部资源，保证离线 |
| 版本 | 无框架、无构建、无 npm | 最简部署 |

### 2.2 总体架构（分层）

```
┌─────────────────────────────────────────────┐
│  视图层 View        (渲染 DOM、事件绑定)       │
│  角色列表 / 角色详情 / 编辑弹窗 / 评估页 / 导入导出  │
│  反馈层 Feedback (feedback.js：模态框/toast/    │
│  confirm/prompt/info，全站交互反馈统一)         │
├─────────────────────────────────────────────┤
│  状态层 Store       (全局状态 + localStorage 同步) │
├─────────────────────────────────────────────┤
│  算法层 Core        (纯函数，无 DOM 依赖)       │
│  calcArtifactScore / calcPerfect /           │
│  calcCharacterScore / evaluateArtifact        │
├─────────────────────────────────────────────┤
│  数据层 Data        (数据结构、校验、导入导出、外部格式适配、文本识别) │
└─────────────────────────────────────────────┘
```

**核心原则**：算法层是**纯函数**（输入数据 → 输出结果），与定稿算法文档一一对应，便于测试与复用；视图层只负责渲染与交互。

### 2.3 文件结构

```
圣遗物网页/
├── index.html              # 入口（引入各模块 script）
├── assets/
│   └── elements/           # 元素图标（火/水/风/雷/冰/岩/草.png，角色名左侧展示用）
├── css/
│   └── style.css           # 全部样式（设计 token 体系 + 组件样式 + 响应式）
├── js/
│   ├── constants.js        # Umax 表、部位/词条枚举、词条类型（主/副）、别名表（部位/词条/套装）、元素枚举 ELEMENTS + 角色名→元素映射 CHARACTER_ELEMENT_HINTS
│   ├── data.js             # 数据结构校验（validateArtifact/validateRole/...）
│   ├── parser.js           # 文本识别（parseArtifactText：部位/套装/等级/主属性/副词条解析）
│   ├── adapters.js         # 外部格式适配器注册表（detect + convert）
│   ├── storage.js          # localStorage 封装（load/save/migrate）
│   ├── io.js               # JSON 导入导出（探测格式 → 备份导入/适配器转换 → 校验 → 覆盖/合并）
│   ├── core.js             # 算法层：三算法 + 充能 + 潜力（对照算法文档）
│   ├── feedback.js         # 反馈层：增强版模态框 / toast / confirm / prompt / info（统一交互反馈）
│   ├── store.js            # 状态层：角色 CRUD、当前角色切换
│   ├── ui.js               # 视图层：渲染函数 + 事件绑定（openModal/closeModal 委托 feedback.js）
│   └── main.js             # 启动入口、初始化（App.debug 调试接口）
├── test.js                 # 算法/解析器回归测试（Node 环境运行：node test.js，125 项断言）
├── test-dom.js             # UI 冒烟测试（jsdom 无头加载页面，模拟建角色→点进详情→捕获运行时错误；需 workspace 装 jsdom）
└── README.md               # 使用说明（可选）
```

> 无构建环境下，`index.html` 按上述顺序用 `<script src>` 引入，各模块挂到全局命名空间 `App.*`（如 `App.core.calcCharacterScore`）。
> **视图切换**：单页无 hash 路由，由 ui.js 闭包 `view = {type:'list' | 'detail'|'evaluate', ...}` 状态对象驱动 render() 重渲染。
> **弹窗职责划分**：所有模态交互（录入/编辑/配置/评估/确认/输入/信息）统一走 `feedback.js` 的增强版模态框；`ui.js` 的 `openModal/closeModal` 委托至 `App.feedback`，保证动画、Esc/遮罩关闭、焦点管理、滚动锁定等行为全站一致。
> **函数命名约定**：ui.js 闭包内函数不可重名（后定义覆盖先定义，如 `renderArtifactCard` 部位卡 与 `renderPendingCard` 待评估摘要卡 曾重名导致详情页空白）；新增函数前先全局检索重名。

### 2.4 数据模型（JS 结构，与算法文档一致）

```js
// 圣遗物
const Artifact = {
  id: "id_xxx",                // 唯一标识（store.genId()：时间戳36进制+随机）
  slot: "flower",              // flower|plume|sands|goblet|circlet
  set: "绝缘之旗印",            // 所属套装（必填，定稿要求）
  mainStat: "生命值",           // 主词条
  mainValue: 4780,             // 主属性数值（仅展示；外部导入时填充，手动录入可空）
  level: 0,                    // 0~20 整数
  substats: [                  // 副词条，3~4 条
    { stat: "暴击率", value: 7.4, activated: true },  // value：百分比词条用百分数（如 7.4 = 7.4%）
    { stat: "防御力", value: 19, activated: false }   // activated: false = 待激活（4 级解锁）
  ],
  note: ""                     // 备注（可选；莫娜导入填"从莫娜占卜铺导入"）
};

// 角色配置
const RoleConfig = {
  id: "id_xxx",                    // 唯一标识（store.genId()）
  name: "胡桃",
  element: "火",                   // 角色元素（火/水/风/雷/冰/岩/草 或 ""=未选择；角色名左侧显示对应图标）
  looseSlot: "goblet",             // 散件部位（可为 null = 无散件，5 件全套装）
  effectiveStats: ["暴击率","暴击伤害","攻击力百分比"],  // 有效词条
  weights: { "暴击率": 1.0, "暴击伤害": 1.0, "攻击力百分比": 0.5 },  // 权值（仅有效词条；角色配置弹窗默认 1）
  erRequirement: 180,              // 充能需求阈值（%）；仅当「元素充能效率」为有效词条时配置，否则为 null（算法不惩罚/不提示）
  currentER: 200,                  // 当前面板充能（%，含基础100%及武器等）；同上，充能非有效词条时为 null
  artifacts: {                     // 5 部位（未录 = null）
    flower: Artifact, plume: Artifact, sands: Artifact,
    goblet: Artifact, circlet: Artifact
  },
  createdAt: 1723860000000,
  updatedAt: 1723860000000
};

// 全局存储结构（localStorage 的 value，key = "gys_reliquary_v1"）
const StoreSchema = {
  version: 1,                  // 结构版本（迁移用）
  exportedAt: null,            // 导出时间（备份时填充）
  roles: [RoleConfig, ...],
  customSets: ["炉火融炼之心"]  // 自定义套装（文本识别「新套装」确认后追加；随导出/导入）
};
```

### 2.5 数据持久化（storage.js）

```js
const STORAGE_KEY = "gys_reliquary_v1";

App.storage = {
  load()  { /* 读取 + 解析 + JSON.parse 容错；损坏时返回默认空结构并提示 */ },
  save()  { /* JSON.stringify 写入 localStorage */ },
  migrate(raw) { /* 按 version 升级结构（预留）；customSets 必须透传，否则刷新页面后自定义套装丢失 */ }
};
```

- 每次数据变更（增删改角色/圣遗物）后 `save()`，**写失败（超容量）时弹提示并引导导出备份**。
- 读写统一入口，避免散落。
- **migrate 透传 customSets**（`Array.isArray(raw.customSets) ? raw.customSets : []`，emptyStore 同样含空数组）——2026-08-23 修复：此前 migrate 只保留 version/exportedAt/roles，导致自定义套装保存后刷新页面即丢失。
- **首页筛选状态独立持久化**（2026-08-23 起）：key = `gys_reliquary_filter_v{version}`（与角色数据分开）；`loadFilter()`（JSON 解析 + 形状校验：slot/mainStat 字符串、sets 字符串数组，损坏/非法返回 null 回退默认「全部」）/ `saveFilter(f)`（每次筛选变更、清除时落盘，刷新/重新打开恢复上次筛选）。

### 2.6 数据导入 / 导出（两种导入场景）

**导入分为两种场景，自动区分：**

```
选择文件 → 读取文本 → JSON.parse（去 BOM）→ 结构探测（detect）
              ├─ 本项目备份格式（含 version + roles）→ 全量校验 → 覆盖/合并
              └─ 外部格式（其他工具导出）→ 适配器转换（convert）→ 全量校验 → 覆盖/合并 → 提示统计与警告
```

> **实现说明**：转换结果不经过"可编辑确认面板"——直接校验后进入覆盖/合并；外部角色缺有效词条/权值等配置，由导入后提示引导进入「角色配置」手动补充（转换警告在导入成功的信息弹窗中列出）。

#### ① 备份导入（本项目格式）

- 结构：`{ version: 1, exportedAt, roles: [...] }`（可选 `customSets: [...]`）
- 流程：`JSON.parse` → version 兼容检查 → **全量深度校验**（逐角色、逐圣遗物，收集全部错误）→ 导入策略（覆盖 / 合并，重名确认）

#### ② 首次使用导入（外部格式适配器框架）

外部 JSON 由其他圣遗物工具导出，结构未知。**架构决策：适配器在网页内实现**（与"零部署、双击即用"原则一致；无需 Python 环境，避免割裂用户流程；适配器可长期复用——换设备/清缓存/新工具格式都会再次触发外部导入，并非一次性代码）。

适配器注册表将外部结构转换为本项目内部模型：

```js
App.adapters = [
  {
    id: "backup_v1",                        // 本项目备份格式
    detect(raw)  { return raw.version === 1 && Array.isArray(raw.roles); },
    convert(raw) { return raw; }            // 已是内部结构
  },
  {
    id: "external_xxx",                     // 预留：其他工具格式（待用户提供样例后实现）
    detect(raw)  { /* 特征探测：如顶层含 artifacts/characters 字段 */ },
    convert(raw) { /* 映射：外部字段 → 内部 Artifact/RoleConfig 结构 */ }
  }
  // 更多适配器按需注册
];
```

**已实现适配器：莫娜占卜铺（mona-chan）**（2026-08-17，依据 `mona.json` 样例开发）：

| 项 | 说明 |
|----|------|
| detect | version 为字符串 + 含 flower/feather/sand/cup/head 五数组 + 元素含 setName/mainTag/normalTags |
| 部位映射 | flower→flower, feather→plume, sand→sands, cup→goblet, head→circlet |
| 词条映射 | lifeStatic→生命值、attackStatic→攻击力、attackPercentage→攻击力百分比、critical→暴击率、cureEffect→治疗加成 等 19 种 |
| 套装映射 | 42 种英文 key → 中文名（含 6.x 新套装：影中沉凝的幻灭/天之美赐/风起之日/晨星与月的晓歌/纺月的夜歌/穹境示现之夜/深廊终曲） |
| 数值单位 | 百分比词条小数字段 ×100 转百分数（0.041→4.1）；数值词条原值 |
| 装备处理 | 仅导入 `equip` 非空（已装备）件并按角色分组；背包件（1991）不导入（本工具无背包结构），统计提示 |
| 角色配置 | effectiveStats/weights/element 留空（外部格式无此概念），erRequirement/currentER 默认 180，导入后需在角色配置中手动补充（元素可按角色名映射自动预选） |
| 散件部位 | **依据套装分布智能推断**：件数为 1 的套装对应部位即为散件（1 件套无套装效果），装备 ≥4 件时按部位顺序取第一个单件套；覆盖 4+1（异套件）/2+2+1（单件套）/3+1+1；全同 5 件套或装备 <4 → null（无散件，可手动设置）——不再硬编码默认杯 |
| 数据来源 | 全部 4 条副词条已激活（莫娜无 3 初始），level 真实等级（可能非 4 倍数，remaining 公式已兼容） |

- **适配器职责**：`detect`（识别格式）+ `convert`（外部字段 → 内部模型 + 词条/套装/部位名称规范化）
- **新增适配器流程**：拿到外部 JSON 样例 → 分析字段结构 → 实现 detect/convert → 注册
- **角色配置需手动补充**：外部工具一般只含圣遗物数据（5 部位副词条），**不含本项目特有的"有效词条 / 词条权值 / 充能需求 / 面板充能 / 散件部位"配置**——转换后默认留空（effectiveStats=[]，充能 180），导入成功提示用户进入「角色配置」补充后才能得到完整评分
- **导入校验放宽**：`validateStore` 只校验结构合法性（角色名唯一 + 圣遗物数据合法）；角色配置缺失不阻止导入（可后补），与「保存角色配置」时的硬校验（至少一个有效词条）相区分
- 转换后同样走**全量校验**，校验失败会定位到原始数据字段
- 导入成功后展示适配器统计：导入件数 / 背包未导入件数 / 转换警告（未知套装/词条/缺部位等）
- 无适配器匹配时：提示「暂不支持该文件格式，请提供样例以便适配」，并**展示原始 JSON 结构**供人工确认
- **演进边界**：若未来外部格式确实复杂（如 OCR 图片、海量批量清洗、需第三方 API），再评估拆分为独立程序；当前个人场景（单次/低频、零部署优先）维持网页内实现

**导出**（备份，完整 JSON）：
- 组装 `{version, exportedAt, roles, customSets?}` → Blob 下载 `圣遗物备份_YYYY-MM-DD.json`（带 BOM，UTF-8）；覆盖/合并导入时自定义套装随数据迁移（合并 = 并集去重）
- 导出前仅检查角色数非空（数据合法性由录入/编辑时的校验保证；保存失败（超容量）时 storage.save 提示尽快导出备份）

### 2.7 单件圣遗物录入（两种模式）

**模式一：文本识别（默认）** —— 粘贴一段文本自动解析：

```
输入示例：
空之杯雷元素伤害加成7.0%+0·攻击力+4.1%·生命值+4.7%·元素充能效率+6.5%·防御力+19（待激活）角斗士的终幕礼：(0)

识别结果：
部位 = 空之杯 → goblet
主属性 = 雷元素伤害加成（数值 7.0% 仅展示，算法不依赖主属性数值）
等级 = 0（来源：主属性数值后的 +0；文本末尾「(0)」为冗余显示，忽略）
副词条 = 攻击力+4.1% / 生命值+4.7% / 元素充能效率+6.5% / 防御力+19（待激活）→ 3 条激活 + 1 条待激活（初始三）
套装 = 角斗士的终幕礼
```

**识别流程**（`App.parser.parseArtifactText(text)`）——**先合并成一行，再统一识别切割**（分行的位置不确定，不做行结构假设）：

**第 0 步：归一化** —— **删除所有 `★`（星级符号，与识别无关）** → 所有换行统一替换为 `·` 分隔符（任意分行位置都合并成一行）；分隔符集 = 任意空白（含单空格，兼容「生命值 717」名值同行）/ `·` / 顿号 / 逗号 / 分号。

**第 1~2 步：定位部位、套装** —— 部位/套装别名表最长匹配（空之杯→goblet、生之花→flower 等；角斗士的终幕礼 等套装名）。

**第 3 步：主属性 / 等级 / 副词条段划分**（片段分类，不依赖行结构）：
- 取「部位之后 → 套装之前」的内容，按分隔符切分为片段
- **等级依据 `+N` 判断（不依赖星级行 ★，`+` 后面跟着的一定是等级）**：
  - 独立等级片段（整体为 `+N` / `Lv.N`，如 `★★★★★+0` 归一化删 ★ 后为 `+0`、`Lv.12`）→ 从片段流提取并跳过
  - 紧凑格式主属性片段内数值后的 `+N` 尾巴（如 `雷元素伤害加成7.0%+0` → 0）
- **紧凑格式**：首个片段为「词条名+数值(+N)」→ 主属性 = 该片段（名/值/等级），其余为副词条
- **分段格式**：首片段为纯词条名（无数值）→ 前导片段依次为 主属性名 / 主属性数值（纯数值，可带 %），其后为副词条
  - **主属性名歧义消解（结合数值单位）**：主属性名「攻击力/生命值/防御力」且数值带 % → 对应百分比词条（如「时之沙 攻击力 46.6%」→ **攻击力百分比**，不与副词条数值「攻击力」冲突）；数值不带 %（如「死之羽 攻击力 311」）→ 保持数值词条
- 两种格式自动识别切换，识别逻辑共用

**第 4~6 步：副词条拆分 / 初始判定 / 互斥检查**（共用逻辑）：

4. **副词条**：按分隔符拆分，每条解析 `词条名 + 数值(+%)` + `（待激活）` 标记
   - 词条名经**别名表**规范化：攻击力/小攻击→攻击力、攻击力%/大攻击→攻击力百分比、生命值/小生命→生命值、生命值%/大生命→生命值百分比、防御力→防御力、防御力%→防御力百分比、暴击→暴击率、爆伤→暴击伤害、精通→元素精通、充能→元素充能效率、雷伤→雷元素伤害加成 等
5. **初始判定**：副词条总数 = 4 → 初始四（全部激活）；= 3 激活 + 1 待激活 → 初始三；其余 → 提示补全
6. **互斥检查**：副词条之间、副词条与主词条不重复（不重复原则），重复则黄色警告

**词条消歧规则（识别精度关键）**：词条名匹配采用**最长前缀匹配 + 数值单位判定**——

| 词条文本 | 数值带 %？ | 识别结果 |
|---------|-----------|---------|
| 攻击力 +4.1% | 是 | 攻击力**百分比** |
| 攻击力 +19 | 否 | 攻击力（数值） |
| 生命值 +5.8% | 是 | 生命值**百分比** |
| 生命值 +299 | 否 | 生命值（数值） |
| 暴击 +7.4% | 是 | 暴击率 |
| 精通 +42 | 否 | 元素精通 |

- 先做最长词条名前缀匹配（避免"攻击力"误吞"攻击力百分比"），再按数值有无 `%` 判定数值/百分比词条
- 主属性同理：如「雷元素伤害加成7.0%」→ 雷元素伤害加成（词条名）

**识别容错**：
- 识别警告（未知套装/主属性/副词条片段、数量异常、互斥冲突等）以**黄色警告条**逐条列出在识别结果上方
- 识别结果预览表展示：部位/套装/等级/主属性/副词条（含待激活标记）；识别结果自动填入表单，可继续手动修正
- 一键切换到手动模式

**未知套装（自定义套装）识别**：
- **启发式识别**：未匹配到内置/自定义套装时，副词条段末尾的「套装名:(N)」形态片段（如 `炉火融炼之心:(4)`）识别为套装，**不再误报为副词条片段**；返回 `setUnknown=true`
- **首次确认**：识别到新套装（`setUnknown=true`，不在 `C.SETS`/`store.customSets`）→ 弹窗确认「是否加入自定义套装？」；确认后写入 `store.customSets`（store 顶层数组，随导出/导入），并 toast 提示
- **再次识别直接命中**：`parseArtifactText(text, { customSets })` 将自定义套装并入套装候选——第二次识别同类文本直接命中，`setUnknown=false`、零警告
- **表单联动**：套装下拉 = 内置套装 + 自定义套装 + 当前表单值（已填自定义套装可回显），自定义输入框保留

**模式二：手动输入** —— 表单选择（部位/套装/主词条/等级/副词条行），即原方案的表单录入，与文本识别结果互通（可互相转换）。

### 2.7.1 新圣遗物「录入 → 评估 → 保存」流程（临时对象）

新圣遗物录入后**不直接保存**，作为**临时对象**流转：

```
录入（文本识别/手动）→ 临时对象 → 进入价值评估（算法三）
   ├─ 用户确认替换 → 写入角色 artifacts[slot]（旧件直接丢弃），重新计算评分
   └─ 用户放弃     → 丢弃，不落库
```

- 角色每部位只有 1 件，故新圣遗物必须**先评估、后写入**，避免覆盖现有件
- 录入入口：角色详情「＋ 录入新圣遗物」（单角色弹窗评估）与顶栏「价值评估」（全角色评估）——两者都走临时对象流程（部位卡已无"评估替换"按钮，评估统一收敛到这两个入口）
- 临时对象仅在评估会话内存在，不写入 localStorage；确认替换才触发角色更新与重算

### 2.8 校验与报错机制（关键）

用户明确要求"直接在网页中修改并报错"，校验分**硬性（阻止保存/导入）**与**软性（警示不阻止）**两级：

#### 圣遗物（Artifact）校验

| # | 规则 | 级别 | 报错文案示例 |
|---|------|------|-------------|
| 1 | 主词条与部位匹配（花=生命值、羽=攻击力） | 硬 | "花的主词条必须为生命值" |
| 2 | 副词条数量 3~4 条 | 硬 | "副词条数量必须为 3 或 4 条" |
| 3 | 副词条互不重复，且与主词条不重复（不重复原则） | 硬 | "副词条不能与主词条重复：暴击率" |
| 4 | 数值 > 0 | 硬 | "数值必须大于 0" |
| 5 | 数值 ≤ Umax × 6（单条词条最多 6 次 roll 的物理上限） | 硬 | "数值超出该词条的理论上限，请核对" |
| 6 | 百分比词条数值 ≤ 100（百分数口径，如暴击率应填 2.7~3.9 一类百分数） | 硬 | "暴击率为百分比词条，请填百分数（0~100）" |
| 7 | 待激活词条（activated=false）最多 1 条，且须有数值 | 硬 | "待激活词条数量不能超过 1 条" |
| 8 | level ≥ 4 时不允许有待激活词条（4 级已解锁） | 硬 | "4 级以上圣遗物不应存在待激活词条" |
| 9 | level 为 0~20 整数 | 硬 | "等级必须在 0~20 之间" |
| 10 | set（套装）必填 | 硬 | "请选择所属套装" |
| 11 | mainStat 必填 | 硬 | "请选择主词条" |

> 说明：副词条显示值是多次 roll 之和（初始 1 次 + 强化叠加），**可超过单发 Umax**（如暴击率显示值 7.4% > Umax 3.9%，对应算法文档 2.4 示例）。因此不做"≤ 单发 Umax"校验，仅做正值与物理上限（Umax × 6）校验。

#### 角色配置（RoleConfig）校验

| # | 规则 | 级别 | 报错文案示例 |
|---|------|------|-------------|
| 1 | effectiveStats 非空 | 硬 | "至少需要一个有效词条" |
| 2 | weights 覆盖全部 effectiveStats，且 0 < w ≤ 2 | 硬 | "有效词条必须配置权值" |
| 3 | looseSlot ∈ 5 部位 或为空（无散件） | 硬 | "散件部位不合法" |
| 4 | 5 个部位 artifacts 均已录入 | 软 | "还有 N 个部位未录入，评分暂不完整"（允许保存，评分区提示不完整） |
| 5 | erRequirement、currentER ≥ 100 | 软 | "充能需求/面板充能一般不低于 100%" |
| 6 | 角色名非空且唯一 | 硬 | "角色名不能为空 / 已存在同名角色" |
| 7 | 充能是有效词条但 erRequirement/currentER 未填 | 软 | "充能是有效词条，建议填写充能需求与当前面板充能"（不阻止保存） |
| 8 | element 非空时 ∈ 7 元素 | 软 | "元素「X」不在可选范围内" |

#### 报错交互设计（实现现状）

- **录入弹窗**：点「保存」时 `validateArtifact` 汇总全部硬错误 → **信息弹窗（错误样式，红框 + 逐条列表）**列出，阻止保存（无逐字段红框）
- **角色配置弹窗**：点「保存」时 `validateRoleConfig` 汇总硬错误 → 信息弹窗列出；软性提示（未录全部位、充能 <100%）在角色详情评分卡/首页徽章以标签体现；重名等单条错误用 toast 提示
- **识别预览**：解析警告以黄色警告条列出（如互斥冲突、数量异常），保存时校验兜底
- **导入**：`validateStore` 列出每个角色的全部硬错误（角色名 + 字段路径），任一硬错误整体拒绝（信息弹窗展示）；角色配置缺失不阻止导入（可后补）；导入成功以信息弹窗展示统计与警告，关闭后刷新列表

#### 交互反馈体系（feedback.js）

全站所有非表单类交互反馈统一收敛到 `App.feedback`，替代浏览器原生对话框（样式割裂、阻塞流程、移动端体验差）：

| 能力 | 接口 | 说明 |
|------|------|------|
| 增强版模态框 | `openModal(title, body, foot, opts)` | 打开动画（scale+fade）、Esc 关闭、遮罩点击关闭、焦点自动进入弹窗、关闭后焦点还给触发元素、body 滚动锁定；`opts.backdrop=false` 可禁遮罩关闭 |
| 确认对话框 | `confirm({title, message, confirmText, cancelText, danger})` | 返回 Promise：`true`=确认 / `false`=取消 / `null`=Esc·遮罩关闭（放弃）；`danger=true` 时红色强调且默认焦点在「取消」，降低误触 |
| 输入对话框 | `prompt({title, label, placeholder, defaultValue, confirmText})` | 返回 Promise：输入值（Enter 确认）或 `null`（取消） |
| 信息对话框 | `info({title, message, type, confirmText})` | 替代 alert：支持 info/success/error/warn 四种样式 |
| 轻量通知 | `toast(msg, type, duration)` | 右上角滑入、自动消失，不打断流程；success/error/warn/info 四色左边框 |

应用场景：删除角色/替换圣遗物/导入策略（覆盖 or 合并）用 `confirm`；新建角色名用 `prompt`；导出成功/替换成功用 `toast`；校验错误/导入失败用 `info`（error 样式）。

### 2.9 算法层（core.js）

与定稿算法文档逐条对应，全部为纯函数：

```js
App.core = {
  UMAX: { 暴击率: 3.9, 暴击伤害: 7.8, 攻击力百分比: 5.8, 生命值百分比: 5.8,
          防御力百分比: 7.3, 元素精通: 23, 元素充能效率: 6.5, 攻击力: 19,
          生命值: 299, 防御力: 23 },   // 常量表（算法文档 1.3）

  // —— 算法一 ——
  calcArtifactScore(S, role),          // 单件加权有效词条数（仅已激活+有效词条）

  // —— 算法二 ——
  calcPerfect(slot, role),             // 每部位完美词条数 = init + 5×maxW
  calcEnergyPenalty(role),             // 充能超额惩罚（直接全扣）
  calcCharacterScore(role),            // 角色总分 + perfectTotal + percent(封底0) + tags

  // —— 算法三 ——
  maxW(slot, role),                    // 可用有效词条最高权值
  remaining(level),                    // max(0, 5 − floor(level/4))
  ERContrib(artifact),                 // 该件副词条充能贡献（面板口径 %）
  evaluateAgainst(newArtifact, oldArtifact, role),  // 核心比较（潜力+充能+判定+评分）
  evaluateArtifact(newArtifact, role)  // 主入口：情况一/二/三 + 汇总
};
```

实现要点：
- 数值口径：百分比词条 value 与 Umax 同为百分数（如 7.4 = 7.4%），避免当量失真
- `evaluateAgainst` 中的充能重算：`newER = currentER − ERContrib(old) + ERContrib(new)`，`ERContrib = Σ(副词条中充能显示值)`
- **充能未配置时算法三跳过充能约束**（`erRequirement` 非数值）：满级「建议替换」判定不再要求 `newER ≥ erRequirement`，verdict 不出现"替换后充能不足"，替换后评分不扣充能惩罚——与算法二口径一致（充能非有效词条 → w=0 不惩罚）
- 判定与评分解耦、满级/未满级分支、封底 0 分，均照文档实现
- **情况一主词条匹配**：`evaluateArtifact` 的情况一（作为散件）要求**主词条与被替换散件一致**（主词条不同不算替换，不评估）——例如新火伤杯只对"散件杯当前主词条也是火伤"的角色评估；情况二（套装件）本就要求同套装+同主词条。这是对算法文档「主词条校验（可选）」的实现决策：当前以"与被替换件主词条一致"为必要条件（角色级"有效主词条"配置未引入）。
- **情况二排除散件**：`evaluateArtifact` 的情况二（作为套装件）要求该部位**不是散件部位**（`newArtifact.slot !== role.looseSlot`），即必须是套装件——避免同一件散件被同时以"情况一散件"和"情况二套装件"双重评估（角色散件部位的圣遗物不算套装件）。
- **情况三（套装件替换散件）**：`evaluateArtifact` 的情况一中，若新件是**角色所穿套装的部件**（其 set 与角色除散件部位外任一部位 set 相同，`isSetPiece` 判断），则标记 `isSetSwap` 且 case = "散件(变为套装件)"——替换后该部位从散件变为套装件，角色将失去散件（looseSlot 置空）。替换确认时提示该变化；确认后 `role.looseSlot = null`。原神无 5 件套效果，故套装效果不变（若涉及凑齐 4 件套等效果增强，需套装效果数值模型，暂不纳入）。
- **充能联动边界**：`ERContrib` 仅计副词条充能。若替换涉及主词条变化（如充能沙→攻击沙，仅情况一散件可能），主词条充能（51.8% 等）的增减不在联动范围内——需用户在替换后手动更新 `currentER`，或在确认面板提示「主词条充能变化，请核对面板充能」
- **潜力叠加仅限已有副词条（对算法文档 4.2 公式的实现细化）**：强化只能叠加在**圣遗物已有副词条（含待激活）**上，不会凭空出现新词条。故潜力公式的叠加权重 = **该件圣遗物已有副词条中有效词条的最高权值**（`maxWExist`），而非部位级 `maxW(slot)`：
  - `potential = currentScore + pendingContrib + max(0, remaining − |pending|) × maxWExist`
  - 若已有副词条**全部为无效词条**（如防御力/生命值%/防御力%/攻击力 对主暴击爆伤角色），`maxWExist = 0` → 潜力 = currentScore（强化对评分无任何提升）
  - 效果：克洛琳德场景（新沙副词条全无效）潜力 = 0 ✓；示例 B（已有暴击率等有效词条）潜力 6.53 不变 ✓；示例 C（仅攻击力百分比 0.8 有效）潜力由 4×1.0=5.166 修正为 4×0.8=3.766（更贴合游戏实际，不假设能砸出不存在的暴击率）

---

## 三、网页设计

### 3.1 信息架构（单页应用 + 视图切换）

```
┌──────────────────────────────────────────────┐
│  顶栏：Logo（金色品牌图标 + 鎏金字） | 价值评估 | 新建角色 | 导入 | 导出 │
├──────────────────────────────────────────────┤
│  视图 1：角色列表（筛选 + 分数降序 + 单行信息总览卡） │
│    ├ 筛选栏：级联（散件部位 → 主属性 → 套装多选·≥2件）+ 清除  │
│    └ 每行一卡：角色名+总分 | 有效词条+权值 | 5 部位徽章 | 状态徽章 │
├──────────────────────────────────────────────┤
│  视图 2：角色详情（默认视图）                    │
│    ├ 评分总览卡：大分数 + 达成度进度条 + 词条/完美/有效/充能信息块  │
│    ├ 5 部位卡：花/羽/沙/杯/头（主词条、副词条、词条数+达成度%，整卡按达成度分级着色）│
│    └ 右上角按钮组（分组）：导航[返回] · 主操作[角色配置/编辑/录入] · 危险[删除] │
├──────────────────────────────────────────────┤
│  视图 3：圣遗物编辑（模态弹窗）                  │
│    └ 表单：部位/套装/主词条/等级/副词条(词条+数值+激活状态) │
│       等级=0 时副词条数值为档位下拉（四档点选）        │
├──────────────────────────────────────────────┤
│  视图 4：价值评估（独立视图，顶栏入口 · 全角色评估）   │
│    ├ 头部：标题 + 说明 + 新件摘要卡（一行主信息+一行副词条，可「修改」）│
│    └ 下部：汇总（含手动移除数）+ 图例 + 按情况分组（三>二>一，颜色区分） │
│          每行一角色（角色名可点跳转/词条差/潜力/有效词条+权值/替换按钮·仅满级/✕移除）│
│          结果行 grid 固定列（eval-row-grid）：角色名|词条差|潜力|有效词条|替换后分|标签|操作——│
│          所有行共享列轨道纵向对齐，条件列空占位；窄屏(<980px)降级 flex 换行 │
└──────────────────────────────────────────────┘
```

### 3.2 页面 / 组件设计

**① 角色列表（首页 · 筛选 + 分数降序 + 单行信息总览卡片）**
- **排序**：按角色评分百分制**降序**排列（高分在前）
- **级联筛选栏**（列表顶部）：**第一级「散件部位」下拉**（全部/生之花/死之羽/时之沙/空之杯/理之冠）+ **第二级「主属性」下拉**（选散件部位后联动为该部位可选主词条；未选时列出全部主属性合并）+ **第三级「套装」多选下拉**（点击展开面板：**自定义套装置顶** + 内置套装 checkbox 列表，**无件数选择器、无清空按钮**，固定 ≥2 件视为套装；勾选后局部刷新结果区、面板保持打开）+「✕ 清除」（**清除套装勾选仅通过筛选栏「✕ 清除」或逐个取消勾选**）；**三个条件叠加 AND 筛选**：散件部位 = `looseSlot === 所选部位`（无散件角色不命中）；混合 = 散件部位圣遗物主词条匹配；仅主属性 = 任意已录入部位主词条匹配；套装 = 已录入圣遗物中套装 ∈ 所选集合的件数 ≥ 2（2 件套即算套装，固定阈值不提供件数切换）；筛选状态记忆（返回首页保持，存于模块级 `listFilter`，独立于 `view`，进详情/评估页返回后不丢失；并持久化到 localStorage，**刷新/重新打开页面自动恢复上次筛选**，清除筛选同样落盘）
- **主属性候选表**（constants.MAIN_STATS_BY_SLOT，与游戏实际一致）：花=生命值；羽=攻击力；沙=攻击力百分比/防御力百分比/生命值百分比/元素精通/元素充能效率；杯=攻击力百分比/防御力百分比/生命值百分比/元素精通/物理伤害加成/7 种元素伤害加成（**无充能/治疗**）；头=攻击力百分比/防御力百分比/生命值百分比/元素精通/暴击率/暴击伤害/治疗加成
- 筛选时显示结果提示：「筛选 N/M 个角色 · 散件·X · 主属性 · 套装 A、B（≥2件），按分数降序」；无匹配显示空态（含「✕ 清除筛选」按钮作为出口）
- 每角色**一行**一张横卡（role-list 纵向堆叠），行内用**垂直细分隔线**分三段（视觉引导）：
  1. `角色名（元素图标）+ 总分（金色）`（元素图标位于角色名左侧，`role.element` 或按角色名映射 `constants.CHARACTER_ELEMENT_HINTS` 兜底；未配置则不显示）
  2. 有效词条简称+权值（`暴击1 爆伤1 攻击%0.8`，权值金色；**无「有效」标签**；未配置灰字"未配置"）
  3. 5 部位徽章：`火伤 1.90`（主词条简称 + 单件词条数**两位小数**；**顺序固定为花羽沙杯头，无需部位名前缀**，从第几个即可看出部位）；未录入虚线灰"未录"
- **状态徽章固定最右侧**（margin-left:auto，跨卡片纵向对齐），**双字 + 颜色区分**：
  - 达标 = 绿色「充能」
  - 充能不足 = 红色「充能」（title 悬停显示「充能不足，缺 X%」）
  - 缺部位 = 黄色「缺位」（title 悬停显示「未录入 N 个部位」）
- **散件部位强调**：5 部位徽章中，`looseSlot` 对应部位用**金色边框 + 金色文字**（含未录入的空徽章），**无悬停提示**（金色已足够标识）；普通徽章悬停显示「部位 · 主词条 · 词条数」
- **行内分布均匀**：有效词条区**固定宽度 330px**（不弹性占宽、不随角色词条数变化，保证列对齐）；5 部位徽章区**弹性占据剩余空间并居中**（flex:1 + center），配合最右状态徽章，整行分布均匀
- **纵向对齐（跨角色严格列对齐）**：名字列 width 86px（超长省略号）、分数列 width 66px、有效词条区 width 330px、5 部位徽章固定列宽 88px、状态徽章最右——各列在不同角色卡片间起始位置一致
- 主词条**默认有效、不做强调**；左侧 4px 金色装饰条（hover 亮金）；点击整卡进入详情
- **删除入口（仅详情页）**：角色详情页头部「删除」按钮（红色 btn-danger，confirm 确认弹窗、红色强调、默认焦点在「取消」，删除后返回列表）；首页卡片不提供删除按钮（整卡为可点击入口，降低误删风险）
- 有效词条/主词条简称共用 constants.MAIN_SHORT（生命/攻击%/充能/爆伤/X伤 等）
- **空态（教育 + 引导）**：无角色时首页显示图标 + 标题 + 说明 + 主操作按钮「＋ 新建角色」（入口复用 prompt 输入角色名）；筛选无匹配显示「没有符合筛选条件的角色」+「✕ 清除筛选」；评估页无角色同理提供「新建角色」引导

**② 角色详情（核心页）**
- **评分总览卡（角色信息总览，不含具体圣遗物）**：左侧大号百分制分数（鎏金渐变 46px，弱加粗）；右上角金色微光装饰；右侧结构：**顶部达成度进度条**（金色渐变填充条，宽度 = 百分制分数，带 aria progressbar 语义）→ 下方**横向信息小块**（每块 = 小标签在上 + 值在下，块间 44px 均匀间距，flex wrap）：
  - 词条数 X.XX（修正 X.XX）· 完美基准 X.XX · 有效词条（简称+金色权值）· 充能 面板 X% / 需求 X%（不足时黄色小字「缺 X%」；**仅角色配置了充能时显示该块，未配置则隐藏**）
  - **分数下方无徽章**（充能/缺部位不再放徽章）；文字弱加粗（值 400、有效词条 500）；数字统一 `tabular-nums` 等宽
- **5 部位卡**：横向排布（PC）或纵向（手机）。每卡含：
  - 部位名 + 套装名 + 等级（如「空之杯 · 绝缘之旗印 · Lv.20」）
  - 主词条（大号；**数值词条如花·生命值/羽·攻击力不显示 %**，百分比词条带 %，按 STAT_TYPE 判断）
  - 副词条列表（每条：词条名 + 数值 + 当量小字 + [待激活]标记；**副属性/数值/词条数三列 grid 对齐**，当量 **×X.XX 两位小数**）
  - 底部：单件词条数（居左，**两位小数**，实时由算法一算出）+ **部位达成度百分比**（居右，`词条数 ÷ 该部位完美词条数 calcPerfect × 100`）
- **部位达成度边框分级**（整卡边框颜色 = 达成度）：>80% 红 / 70~80% 橙 / 60~70% 紫 / 50~60% 蓝 / <50% 绿（百分比文字同色 + 微光晕；完美词条数为 0 时按 0% 绿）
  - **无编辑/评估按钮**（编辑统一走右上角「编辑」→ 部位选择弹窗；评估走顶栏「价值评估」独立页）
- **头部**：仅角色名（**无散件/有效词条副标题文字说明**；角色名左侧显示元素图标，`role.element` 或角色名映射兜底，未配置不显示）；右上角**操作按钮分组**（三组视觉分离，降低认知负担）：
  - 导航组（幽灵按钮）：`← 返回`
  - 主操作组：`⚙ 角色配置` / `✎ 编辑`（弹出**部位选择弹窗**：5 个部位按钮，显示当前主词条简称，未录部位虚线「（未录）」，点选后进入该部位编辑弹窗预填）/ `＋ 录入新圣遗物`（鎏金主操作）
  - 危险组（红色）：`🗑 删除`
- **空部位卡**：虚线框「＋ 生之花」，点击直接进入该部位录入弹窗（"new" 模式）；散件部位空卡为金色虚线
- **散件部位视觉引导**（不文字说明）：部位卡主词条名旁金色「散」角标（.a-loose-tag）；未录散件部位空卡金色虚线边框

**③ 圣遗物录入（双模式）**
- **文本识别模式**：顶部大文本框（**.artifact-text：深色主题、min-height 150px、可拖拽调高**，与网页风格一致），粘贴圣遗物文本 → 「识别」→ 下方展示解析结果（部位/套装/等级/主属性/副词条+待激活标记），无法识别片段黄色高亮，逐项可修正 → 「确认保存」；提供「切换手动模式」入口
- **手动模式**：部位（下拉，切换时主词条候选随之变化）、套装（下拉 + 自定义）、主词条（按部位过滤）、等级（0~20）、副词条 4 行（每行：词条下拉 + 数值 + 激活开关，可增删行）
  - **数值输入联动**：等级 = 0 时，副词条数值控件为**档位下拉**（5★ 单次 roll 四档：如暴击率 2.7/3.1/3.5/3.9，直接点选；选词条后档位随之刷新）；等级 > 0 时为数字输入（强化后为多次 roll 之和，无固定档位）
  - **副词条控件样式**：词条下拉/数值输入为**深色主题、height 34px、圆角 6px**（与网页风格一致）
  - **副词条互斥（不重复原则）**：每个副词条下拉的候选 = 副属性全集 − 主词条（若属于副属性）− 其他行已选词条；任一副词条选中后，其他行的下拉自动排除该词条；主词条变化时同步刷新全部副词条候选
- **文本识别互斥提示**：识别结果若副词条之间重复或与主词条重复，预览区黄色警告「副词条重复：X（不重复原则，请修正）」，保存时校验兜底拦截
- **预填已有数据**：编辑已穿戴件 / 修改评估中的新圣遗物时，表单**自动回填**原数据（prefill 支持圣遗物对象或布尔；`true` 取角色该部位已穿戴件），并**默认进入手动模式**直接展示已填内容，便于修改；文本模式仍可切换重新粘贴
- **一键清空**：底部「清空」按钮，**直接**重置表单为空白（无确认框，文本区与手动表单同步清空）
- 实时校验：保存时 `validateArtifact` 汇总硬错误 → **信息弹窗（错误样式，红框逐条）**列出（重复词条/数值超限/待激活规则等），阻止保存
- 底部按钮：`清空`（直接重置，无确认）/ `取消` / `保存`（编辑已穿戴件）或 `保存并评估`（新圣遗物临时对象）
- 新圣遗物场景：从角色详情「录入新圣遗物」进入 → 录入为**临时对象** → 自动进入价值评估 → 确认替换才写入角色（见 2.7.1）

**④ 价值评估（独立视图 · 全角色评估）**
- 顶部：标题 + 说明（录入新圣遗物后自动对**全部角色**评估）
- 中部：无待评估件时显示「＋ 录入新圣遗物」入口（复用双模式录入弹窗）；有临时对象时顶部显示**新件摘要卡**（主信息一行 + 副词条一行，紧凑，可「修改」）
- 下部：
  - **汇总条**：「✓ 评估 N 个角色：M 个角色有提升」/「✗ 均无提升」
  - **图例**：色块 = 使用方式（金色=套装件替换散件·最高优先 / 紫色=套装件替换 / 灰蓝=散件替换）
  - **按情况分组显示（优先级 三 > 二 > 一）**：先展示所有「情况三（套装件替换散件）」提升，再「情况二（套装件替换）」，最后「情况一（散件替换）」；各组以**颜色**区分（左侧色条 + 同色边框），**不显示「情况 X」文字描述**
  - **每行一个角色（紧凑）**：**角色名（可点击跳转该角色详情页，评估临时对象保留、返回评估页结果仍在）** + 词条差（绿/红）+ 潜力（现 X，剩 N 次）+ **有效词条+权值**（词条简称+金色权值，风格同首页，如 `暴击1 爆伤1 攻击%0.8`）+ 替换后评分（满级才有）+ 替换按钮；一行一条，页面可显示更多结果
  - **手动移除有提升角色**：每行末尾低调「✕」按钮（hover 变红），点击将该角色从本次评估的"有提升"列表移除（`evalHidden` 会话内记录，重新录入/放弃/替换后自动清空）；汇总条与"其余 N 个角色无提升"提示中标注「（已手动移除 N 个）」；全部移除后显示"已手动移除全部有提升角色，暂无更多提升项"
  - **替换按钮仅满级显示**：新圣遗物等级 = 20 时每行展示「替换」按钮（替换后旧件丢弃）；未满级无替换按钮（也不提示）
  - 无提升角色仅提示数量，不展开
  - 操作：「放弃」（清空临时对象）
- 角色详情「＋ 录入新圣遗物」走单角色评估弹窗流程（复用 buildEvalContent）：同样**仅满级显示「确认替换」**；弹窗结果卡同样用**颜色**区分情况类别（无「情况 X」编号，仅短类别词：散件替换 / 套装件替换 / 套装件替换散件），并保留判定徽章（弹窗列出全部情况含不值得的，徽章有区分意义）
- 未满级圣遗物**无任何提示文案**（无「未满级仅评估强化价值」提示），仅表现为无替换按钮

### 3.3 表单字段映射

| 表单 | 字段 | 控件 | 约束/联动 |
|------|------|------|-----------|
| 角色配置 | 角色名 | 文本框 | 必填、唯一 |
| | 元素 | 下拉(7 元素 + 未选择) | 可选；默认按角色名映射自动预选（constants.CHARACTER_ELEMENT_HINTS），保存后落盘；未选择则不显示图标 |
| | 散件部位 | 下拉(5 + 无散件) | 必选或「无散件」（looseSlot 可空，见 2.4） |
| | 有效词条 | 多选 chips | 至少 1 个 |
| | 词条权值 | 数字（每个有效词条） | 0<w≤2 |
| | 充能需求 | 数字(%) | ≥100；**仅当「元素充能效率」为有效词条时可编辑**——否则禁用置空，保存为 null（算法不惩罚/不提示）；勾选充能时若为空自动填 180 |
| | 面板充能 | 数字(%) | ≥100；同上，与充能需求联动禁用/启用 |
| 圣遗物（文本识别） | 粘贴文本 | 大文本框 + 「识别」 | 识别失败片段黄标，可手动修正 |
| 圣遗物 | 部位 | 下拉(5) | 编辑模式也可切换（保存时校验主词条-部位匹配） |
| | 套装 | 下拉(常用套装)+自定义 | 必选 |
| | 主词条 | 下拉 | 按部位过滤候选 |
| | 等级 | 数字/滑条(0~20) | 整数；≥4 时禁用待激活 |
| | 副词条×4 | 词条下拉 + 数值 + 激活开关 | 不重复、0<值≤Umax×6、待激活≤1；**等级=0 时数值控件为档位下拉**（四档点选，词条联动刷新） |

### 3.4 展示设计细节

#### 设计系统（Design Token）

所有颜色、字号、间距、圆角、阴影、动效统一收敛为 `style.css` 顶部 `:root` 的 CSS 变量（一处修改全站生效，为后续主题切换预留）：

| 类别 | Token | 值（示例） |
|------|-------|-----------|
| 品牌色 | `--brand / --brand-strong / --brand-dim` | 鎏金 `#d9b64a / #f0d078 / #a8862e` |
| 背景 | `--bg-0 ~ --bg-3` | 岩层深色系 `#131019 / #1c1826 / #262136 / #302a45`（页面底色 / 卡片 / 输入框 / 悬停） |
| 描边 | `--line-1 / --line-2` | `#332c48 / #473d66` |
| 文字 | `--text-1 / --text-2 / --text-3` | 暖白 `#efe9dc` / 次级 `#b5aec3` / 弱化 `#8b839c` |
| 语义色 | `--green / --yellow / --red / --orange / --purple / --blue / --gray` | 有效 / 警示 / 错误 / 达成度分级等 |
| 数字字体 | `--font-num` | `JetBrains Mono / Cascadia Mono / Consolas`（评分、词条数用，配合 `tabular-nums` 防跳动） |
| 间距 | `--sp-1 ~ --sp-8` | 4px 基准（4/8/12/16/20/24/32） |
| 圆角 | `--r-sm / --r-md / --r-lg / --r-pill` | 6 / 10 / 14 / 999px |
| 阴影 | `--shadow-sm/md/lg` + `--glow-gold/red` + `--hi-line` | 悬浮投影、金色/红色光晕、顶部 1px 高光 |
| 动效 | `--t-fast/md/slow` + `--ease` | 140 / 260 / 420ms + `cubic-bezier(0.4,0,0.2,1)` |

- **背景**：页面底色 `#131019` 上叠加三层径向光斑（右上紫 `rgba(155,123,216,.10)` / 左上金 `rgba(217,182,74,.055)` / 底部蓝 `rgba(91,155,213,.075)`，`background-attachment: fixed`），营造"深邃岩层 + 远处光源"氛围
- **卡片质感**：所有卡片带 1px 顶部高光（`inset 0 1px 0 rgba(255,255,255,.06)`）；悬浮态上浮 2px + 投影 + 对应光晕
- **顶栏**：sticky 固定 + 毛玻璃（`backdrop-filter: blur(10px)` 带降级）；底部 1px 金色微光分隔线；Logo = 金色渐变圆角图标（✦）+ 鎏金渐变文字（`background-clip: text`）

#### 组件规范

- **按钮体系**（`--t-fast` 过渡 + `:focus-visible` 金色描边）：
  - 主操作 `.btn-primary`：鎏金渐变 + 深色文字 + 投影，唯一视觉焦点
  - 次级 `.btn`：深灰渐变 + 描边，hover 亮金边框
  - 危险 `.btn-danger`：红色文字 + 红色描边 + 红色微光晕
  - 幽灵 `.btn-ghost`：透明背景，用于导航类弱操作（如「返回」）
  - 尺寸：`btn-sm / btn-md / btn-lg`；`btn-group` 用于详情页操作分组
- **徽章体系**（`.badge`）：胶囊形 + 1px 描边 + 半透明底（green/yellow/red/gray 四色）
- **模态框**：打开动画（scale 0.94→1 + fade，260ms ease）；头部 sticky + 渐隐底；关闭按钮 hover 变红；操作区 sticky 底部
- **Toast**：右上角堆叠，滑入淡出，左边框按类型着色，`aria-live="polite"`，自动消失不打断流程
- **空状态**（`.empty`）：圆形渐变图标底 + 标题 + 说明 + CTA 按钮（教育 + 引导双目标）

#### 明细规则

- **配色**：深色岩纹背景 + 鎏金强调（贴合原神 UI）；卡片无固定金色描边，颜色由部位达成度边框承载；部位徽章统一中性底，仅散件部位金色强调（不做按部位着色）
- **状态徽章优先级**（首页最右徽章，互斥）：缺部位（黄「缺位」）> 充能不足（红「充能」）> 充能达标（绿「充能」）> **未配置充能（灰「无充」）**；均以 title 悬停显示细节。未配置充能（有效词条不含「元素充能效率」）时以中性灰色「无充」占位（不误导读数，且与「充能/缺位」同为 2 字，跨角色列对齐）
- **情况优先级颜色（评估结果）**：金色（case-3 套装件替换散件）> 紫色（case-2 套装件替换）> 灰蓝（case-1 散件替换），以左侧色条 + 同色边框区分，颜色体现优先级
- **数值高亮**：有效词条数值绿色加粗，无效词条灰色；待激活词条半透明 + 「待激活」徽章
- **当量小字**：每条副词条右侧显示「×0.897」（当量，两位小数）；副属性/数值/当量三列 grid 固定列宽对齐
- **充能标签**：首页状态徽章 = 达标绿「充能」/ 不足红「充能」（title 显示「充能不足，缺 X%」）/ 缺部位黄「缺位」（title 显示未录数量）/ 未配置充能灰「无充」（title 显示「未配置充能（有效词条不含元素充能效率），评分不涉及充能」）；详情页评分卡不显示徽章，充能不足改为信息块内黄色小字「缺 X%」（仅配置充能的角色显示充能块）
- **部位达成度边框分级**（详情页部位卡，整卡边框 = 达成度）：达成度 = 单件词条数 ÷ 该部位完美词条数 `calcPerfect(slot)` × 100（完美为 0 时按 0%）；>80% 红 / 70~80% 橙 / 60~70% 紫 / 50~60% 蓝 / <50% 绿——2px 四边统一边框 + 百分比文字同色 + 微光晕，卡片背景不染色（避免杂乱）
- **视图切换动画**：`render()` 时主区域 260ms 淡入 + 6px 上移
- **滚动条**：金色细条 thumb（WebKit + `scrollbar-color` 双写）
- **减弱动效**：`@media (prefers-reduced-motion: reduce)` 下动画/过渡时长归零（无障碍）
- **favicon**：内联 SVG 金色角标（data URI，无外部资源）；`<meta theme-color>` 匹配页面底色
- **响应式断点**（三段）：
  - `≤900px`：角色卡片有效词条区/部位徽章区取消固定宽度（改自适应换行）
  - `≤768px`：5 部位卡单列堆叠、编辑弹窗全屏化（`max-width:100%`）、评分总览信息块整行、详情操作按钮收紧
  - `≤480px`：顶栏按钮整行均分（flex:1）、部位选择弹窗单列、副词条行收窄
  - 常规（≥768px）：详情 5 部位卡多列（`auto-fill minmax(225px,1fr)`）

### 3.5 交互流程（用户路径）

```
首次使用：新建角色 → 填写角色配置（有效词条/权值/充能）→ 录入 5 件圣遗物（文本识别或手动）→ 看到评分
          或：导入外部 JSON（适配器转换）→ 直接获得角色数据
日常使用：打开即见角色列表 → 进详情看评分 → 打到新圣遗物 → 顶栏「价值评估」→ 文本粘贴识别 → 看哪些角色有提升（按情况三>二>一颜色分组）→ 满级则替换/放弃
定期备份：顶栏「导出」下载 JSON；换设备「导入」恢复
```

---

## 四、功能对照表

| 功能 | 实现模块 | 页面/入口 |
|------|---------|----------|
| 1. 导入/导出 | io.js + adapters.js + storage.js | 顶栏按钮（备份导出 / 导入：自动探测 备份格式 or 外部适配器） |
| 2. 单件词条计算 | core.calcArtifactScore | 角色详情每部位卡底部词条数（+达成度%，实时）；首页 5 部位徽章同步显示 |
| 3. 角色评分 | core.calcCharacterScore | 角色详情评分总览卡 |
| 4. 价值评估 | core.evaluateArtifact | 价值评估视图（录入新圣遗物触发） |
| 5. 数据与结果展示 | ui.js 全部视图 | 列表/详情/评估，实时刷新 |
| 附：文本识别录入 | parser.js | 圣遗物录入弹窗（默认模式） |

---

## 五、开发计划

| 阶段 | 内容 | 产出 |
|------|------|------|
| 1 | 常量与数据结构（constants.js / data.js） | 枚举、Umax 表、别名表（部位/词条/套装）、校验函数 |
| 2 | 存储与导入导出（storage.js / io.js / adapters.js） | 持久化 + 备份恢复 + 外部格式适配器框架（映射待样例实现） |
| 3 | 文本识别（parser.js） | 圣遗物文本解析 + 别名规范化 + 容错 |
| 4 | 算法层（core.js） | 三算法纯函数 + 单元测试（浏览器控制台断言） |
| 5 | 状态层（store.js） | 角色 CRUD、当前角色 |
| 6 | 视图层（feedback.js + ui.js + style.css） | 反馈层（模态/toast/confirm/prompt/info）+ 四个视图 + 双模式录入弹窗 + 校验 UI |
| 7 | 联调与验收 | 对照算法文档示例逐条验证数值 |

**测试策略**：算法层为纯函数，可在浏览器控制台跑断言，复用算法文档 3.6 / 4.5 的示例数据（36 分 / 7.4 / 6.53 / 3.766 等）作为回归基准；文本识别用需求方样例（角斗士的终幕礼 空之杯文本 + 多行字段格式文本）验证解析结果。

> 编码实现已落地：`test.js` 提供 Node 环境回归测试（`node test.js`，**125 项断言**：算法文档示例数值 + 文本识别（单行/多行/删★/等级+N/主属性歧义消解/互斥）+ 词条消歧 + 校验函数 + 莫娜适配器转换 + 散件智能推断 + 情况二/三识别 + 潜力仅叠加已有副词条 + 0 级档位表 + data.js 重复校验），实现与文档不符处已同步更新本方案文档（见文件结构补充 test.js）。

---

## 六、边界与限制

1. **localStorage 容量**约 5MB：当前数据结构（角色×圣遗物）远用不满；写失败时提示导出备份。
2. **仅本机使用**：数据存当前浏览器；换设备/清缓存需手动导出导入。
3. **无后端**：不支持多端实时同步；如需可后续加"导出分享链接"或部署静态托管。
4. **浏览器兼容**：现代浏览器（Chrome/Edge/Firefox/Safari 近两年版本）。
5. **校验口径**：数值上限按定稿 Umax；游戏内实际数值为档位组合，网页只做范围校验，不做离散档位强校验（软性提示偏高项）。
6. **算法边界**：完美态基准按 4 初始；3 初始圣遗物评分天然略低（符合游戏实际，见算法文档第 10 条）。
7. **外部导入**：莫娜占卜铺适配器已实现（2026-08-17，基于 mona.json 样例，42 套装/19 词条/5 部位映射，125 项回归测试覆盖）；其他工具格式仍待样例后扩展，无适配器时提示并展示原始结构；外部格式一般不含角色配置（有效词条/权值/充能），需导入后手动补充。
8. **文本识别容错**：识别基于别名表与正则，异常文本（未知套装/简称/格式变体）会黄标提示并允许手动修正，不保证 100% 识别；**先删除所有 ★、归一化换行合并成一行再识别切割**（分行位置不确定，不做行结构假设），**等级一律依据 `+N` 判断**（独立 `+N`/`Lv.N` 片段，或主属性数值后 `+N` 尾巴）；**主属性名歧义消解**——「攻击力/生命值/防御力」名 + 数值带 % → 对应百分比词条（不与其数值词条冲突），数值不带 % → 数值词条（如羽攻击力 311）；主属性数值不参与算法（仅展示）；文本中其他 `(N)`（如套装名后）为冗余显示，忽略。
9. **词条消歧**：词条名最长前缀匹配 + 数值单位（`%` 有无）判定，区分数值/百分比词条（如 `攻击力+4.1%` → 攻击力百分比，`攻击力+19` → 攻击力）。
10. **新圣遗物为临时对象**：录入后不直接保存，先进入价值评估；确认替换才写入角色（旧件丢弃），放弃则不入库（见 2.7.1）。
11. **充能联动边界**：面板充能联动仅按副词条充能差计算；主词条变化（如充能沙→攻击沙）时主词条充能增减不在联动范围，需用户手动更新 currentER。
12. **0 级档位选择**：等级 = 0 时副词条数值控件为档位下拉（5★ 单次 roll 四档显示值，存于 constants.ROLL_TIERS，第 4 档 = Umax，如防御力百分比 5.1/5.8/6.6/7.3）；等级 > 0 恢复数字输入（强化后为多次 roll 之和，无固定档位）。档位为游戏内四舍五入显示值，与算法数值口径一致。
13. **仅满级可替换**：价值评估（独立页与角色详情弹窗）的替换按钮（「替换」/「确认替换」）**仅在新圣遗物满级（level=20）时显示**；未满级只输出强化判定（值得强化/不值得强化，判定基于"潜力 ≥ 现有散件"），**不提供替换入口、也无提示文案**——与算法三「未满级不做替换判定」的设计一致。
14. **潜力仅叠加已有副词条**：强化只能叠加在圣遗物**已有副词条（含待激活）**上（游戏机制），叠加权重取"已有副词条中有效词条的最高权值"，全部无效则潜力 = 当前词条数（如克洛琳德场景全无效副词条 → 潜力 0）；不假设能砸出圣遗物中不存在的词条（见 2.9 实现要点）。
